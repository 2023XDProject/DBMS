/*
#include<iostream>
#include<fstream>
#include<string>
#include<vector>
#include "SQLexpression.h"
*/
/////以下是可能会用到的解析使用方法
/*
std :: string str="";
    do{
        getline(std :: cin,str); 获取命令字符串
        SqlEx se(str);      用命令字符串作为构造器参数
        if(se.check()){    进行匹配和检查
            vector vec = null;
            string vec=se.getResult();  获得解析结果                  
            for(int i=0;i<vec.size();i++){
                    for(int j=0;j<vec[i].size();j++){
                    cout<<vec[i][j]<<endl;    打印解析结果
                }
                cout<<endl;
            }
        }
    }while (str != "#");
    
*/